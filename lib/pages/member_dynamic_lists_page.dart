import 'package:flutter/material.dart';
import '../pages/dynamic_lists_home_page.dart';

/// Page membre pour accéder aux listes dynamiques
class MemberDynamicListsPage extends StatelessWidget {
  const MemberDynamicListsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DynamicListsHomePage();
  }
}